# Qwen2.5-VL Fine-tuning Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
torch>=2.0.0
transformers>=4.37.0
datasets>=2.14.0
accelerate>=0.25.0
peft>=0.7.0
bitsandbytes>=0.41.0
trl>=0.7.0

# Unsloth (install from GitHub)
# Install separately with: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# Image processing
pillow>=10.0.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Optional: Logging and monitoring
# wandb>=0.15.0

# Optional: Jupyter notebook support
# ipywidgets>=8.0.0
# jupyter>=1.0.0
